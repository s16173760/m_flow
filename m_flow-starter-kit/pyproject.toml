[project]
name = "m_flow-starter"
version = "0.1.1"
description = "Starter project which can be harvested for parts"
readme = "README.md"

requires-python = ">=3.10, <=3.13"

dependencies = [
    "m_flow>=0.1.38,<1.0.0",
]
